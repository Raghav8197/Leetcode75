class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        n_len = len(nums)
        print(n_len)
        for x in range(0,n_len,1):
            print(nums[x])
            if(nums[x]==0):
                nums.remove(0)
                nums.append(0)
